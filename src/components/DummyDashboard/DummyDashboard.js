import React, { PropTypes, Component } from 'react';

class DummyDashboard extends Component{
  render() {
    return (
      <div>
        <h1>Dashboard</h1>
        <textarea>React started kit seems to be able to run only on domain root. When using this ProxyPass, main page is loaded correctly, but other requests are on domain root.
I changed path of app.js to publicPath+"/app.js" in the Html class (https://github.com/kriasoft/react-starter-kit/blob/master/src/components/Html/Html.js#L45), but there are more problems I can't solve.</textarea>
        <p>GeoNode supports numerous CSW backends.  In the non-pycsw case it connects to GeoNetwork or deegree3 via HTTP (see [1] for more information on configuration).</p>
      </div>
    );

  }
}

export default DummyDashboard;
