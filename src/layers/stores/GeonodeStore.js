/**
 * It returns all layers duplicated from the geonode layers. It is possible to manipulate and create them here.
 */
class GeonodeStore {

}

let storeInstance = new GeonodeStore();

export default storeInstance;
